server:
  port: 8080

spring:
  flyway:
    enabled: true
    default-schema: migrations
    schemas: public
    locations: classpath:db/migration/postgresql
    
  data:
    redis:
      url: localhost
      port: 6379

  cache:
    type: redis

  datasource:
    url: jdbc:postgresql://localhost:5432/cognitia
    username: postgres
    password: qwerty123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc.batch_size: 20
        order_updates: true
        order_inserts: true
        default_schema: public
    generate-ddl: true
    show-sql: true   

storage:
  cloudinary_url: cloudinary://351844175262129:totRi-4ZdF_Lxbz9XlTk6SUmc84@dfckaryif 



    


